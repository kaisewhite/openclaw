# HEARTBEAT.md - Architect Agent

- Check assigned `Backlog` and `DONE` tickets for stale states.
- If architecture analysis is complete but the ticket is not moved to `Todo` with an implementation owner, fix it now.
- If a `DONE` ticket has merge-ready PRs, merge or post the exact merge blocker now.
- If repo scope or routing is still ambiguous after 20 minutes, post the blocking question and name the owner who must answer.
- If nothing needs attention, reply `HEARTBEAT_OK`.
